```mermaid
gantt
    title <%= start_date %> - <%= end_date %>
    <% if data_list %>
      <% data_list.each do |hash| %>
        <% next unless hash %>
        <% hash.each do |section, x %>
    section <%= section %>

    <%= x.name %>: <%= %>, <%= x.time%>, <%= x.time_span_with_unit%>
    <% end %>
    <% end %>
```
